## Diseño de las Herramientas MCP

El servidor MCP expone dos tipos de herramientas para atender las solicitudes formuladas por el usuario a través del agente de inteligencia artificial. El primer tipo corresponde a **tools específicas**, que implementan lógica de negocio particular para resolver consultas analíticas o de procesamiento complejo sobre los datos operativos (por ejemplo, análisis de distribución de valores en formularios de campo). El segundo tipo corresponde a **collections**, que exponen de manera genérica los modelos de datos existentes del sistema para su consulta y filtrado, mediante clases base reutilizables que estandarizan el acceso a través de operadores comunes (`$match`, `$sort`, `$limit`). Ambos tipos de herramientas son registrados por el servidor MCP y se encuentran sujetos a los mecanismos de autenticación y autorización descritos en la sección de diseño de seguridad.

| Tool                         | Propósito                                                                                                                         | Parámetros                                                                 | Retorno                       | Scope                              | Permisos                           |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ----------------------------- | ---------------------------------- | ---------------------------------- |
| `ping`                       | Verificar conectividad y estado del servidor MCP                                                                                  | Ninguno                                                                    | `dict[str, str]`              | N/A — endpoint de metadata         | N/A                                |
| `get_server_instructions`    | Obtener instrucciones globales del servidor: colecciones, campos, zonas horarias, terminología                                    | Ninguno                                                                    | `str`                         | N/A — endpoint de metadata         | N/A                                |
| `get_assignment`             | Obtener el estado completo de una asignación, incluyendo relaciones, historial y valores de formulario                            | `assignment_id: UUID`                                                      | `dict`                        | `assignments:read`                 | `view_assignments_all`             |
| `get_user`                   | Obtener un usuario del workspace por UUID                                                                                         | `user_id: UUID`                                                            | `dict`                        | `users:read`                       | `view_users_all`                   |
| `get_user_by_email`          | Obtener un usuario mediante coincidencia exacta de correo                                                                         | `email: str`                                                               | `dict`                        | `users:read`                       | `view_users_all`                   |
| `list_users`                 | Listar usuarios del workspace ordenados por nombre                                                                                | `limit: int = 100 (máx. 500)`                                              | `dict` (lista + conteo)       | `users:read`                       | `view_users_all`                   |
| `search_users`               | Buscar usuarios por nombre, apellido, correo o teléfono                                                                           | `query: str, limit: int = 50 (máx. 200)`                                   | `dict` (lista + conteo)       | `users:read`                       | `view_users_all`                   |
| `get_forms_context`          | Listar formularios del workspace con títulos de campos visibles                                                                   | Ninguno                                                                    | `list[dict]`                  | `assignments:read`                 | `view_assignments_all`             |
| `get_form_schema`            | Obtener esquema semántico simplificado de un formulario                                                                           | `form_id: str`                                                             | `dict` o error                | `assignments:read`                 | `view_assignments_all`             |
| `analyze_field_distribution` | Calcular frecuencias o distribuciones multidimensionales de campos de formulario, con filtro opcional                             | `form_id: str, target_field_ids: list[str], filter_field_id, filter_value` | `dict` anidado                | `assignments:read`                 | `view_assignments_all`             |
| `query_data_collections`     | Consultar de forma genérica cualquiera de las 22 colecciones registradas del sistema, mediante un pipeline de operadores estándar | `collection: str, search_pipeline: list[dict]`                             | `dict`/`list` según colección | Depende de la colección consultada | Depende de la colección consultada |

## Catálogo de colecciones

La herramienta `query_data_collections` permite consultar de forma genérica cualquiera de las 22 colecciones registradas en el sistema, mediante un pipeline de operadores restringido de estilo MongoDB (`$match`, `$sort`, `$limit`, `$skip`, `$project`, `$group`, `$lookup`). El acceso a cada colección está sujeto a su propio scope OAuth y permiso de workspace, definidos de forma independiente según el dominio de datos que expone. Las colecciones con formato de salida `csv` retornan un recurso MCP adjunto (MIME `text/csv`), mientras que el resto retorna los registros como JSON embebido en la respuesta.



| Colección                       | Propósito                                                                 | Formato | Retorno     | Scope                | Permisos                    |
| ------------------------------- | ------------------------------------------------------------------------- | ------- | ----------- | -------------------- | --------------------------- |
| `assignment`                    | Consultar, filtrar, agrupar o contar tareas asignadas a usuarios de campo | json    | JSON inline | `assignments:read`   | `view_assignments_all`      |
| `assignmentexport`              | Exportar asignaciones a archivo descargable                               | csv     | Recurso CSV | `assignments:read`   | `view_assignments_all`      |
| `assignmentstatus`              | Consultar catálogo global de estados válidos                              | json    | JSON inline | `assignments:read`   | `view_assignments_all`      |
| `assignmentstatushistory`       | Consultar historial de cambios de estado                                  | json    | JSON inline | `assignments:read`   | `view_assignments_all`      |
| `client`                        | Consultar clientes/cuentas visitados por agentes de campo                 | json    | JSON inline | `clients:read`       | `view_clients_all`          |
| `clientexport`                  | Exportar clientes a CSV                                                   | csv     | Recurso CSV | `clients:read`       | `view_clients_all`          |
| `place`                         | Consultar ubicaciones físicas de clientes                                 | json    | JSON inline | `clients:read`       | `view_clients_all`          |
| `project`                       | Consultar agrupaciones lógicas de asignaciones                            | json    | JSON inline | `projects:read`      | `view_projects_all`         |
| `form`                          | Consultar plantillas de formularios dinámicos                             | json    | JSON inline | `assignments:read`   | `view_assignments_all`      |
| `tag`                           | Consultar etiquetas de categorización                                     | json    | JSON inline | `tags:read`          | `view_tags_all`             |
| `team`                          | Consultar equipos de campo y responsables                                 | json    | JSON inline | `teams:read`         | `view_teams_all`            |
| `teamusers`                     | Consultar membresías entre equipos y usuarios                             | json    | JSON inline | `teams:read`         | `view_teams_all`            |
| `workspace`                     | Obtener metadatos del workspace actual                                    | json    | JSON inline | `users:read`         | No declarado                |
| `geofence`                      | Consultar límites geográficos con alertas                                 | json    | JSON inline | `geofences:read`     | `view_geofences_all`        |
| `geofencerecord`                | Consultar eventos de entrada/salida en geofences                          | json    | JSON inline | `geofences:read`     | `view_geofences_all`        |
| `alert`                         | Consultar reglas de alertas y notificaciones                              | json    | JSON inline | `alert:read`         | `view_alert`                |
| `totalstats`                    | Consultar estadísticas diarias preagregadas                               | json    | JSON inline | `dashboard:read`     | No declarado                |
| `checkincheckout`               | Consultar visitas con check-in/out, GPS, fotos                            | json    | JSON inline | `checkins:read`      | `view_checkin_checkout_all` |
| `checkincheckoutexport`         | Exportar check-ins/outs a CSV                                             | csv     | Recurso CSV | `checkins:read`      | `view_checkin_checkout_all` |
| `userlocation`                  | Consultar última ubicación GPS conocida                                   | json    | JSON inline | `locations:read`     | `view_checkin_checkout_all` |
| `userlocationexport`            | Exportar últimas ubicaciones a CSV                                        | csv     | Recurso CSV | `locations:read`     | `view_checkin_checkout_all` |
| `userlocationdailyrecord`       | Consultar resúmenes GPS diarios (distancia, velocidad, geofences)         | json    | JSON inline | `daily_records:read` | `view_checkin_checkout_all` |
| `userlocationdailyrecordexport` | Exportar resúmenes GPS diarios a CSV                                      | csv     | Recurso CSV | `daily_records:read` | `view_checkin_checkout_all` |
